import java.util.Scanner;

public class StringQue {
	public static void main(String[] args) {

		/*
		 * Check Palindrome: Write a function that checks if a given string is a
		 * palindrome (reads the same backward as forward). Example: "racecar" -> true
		 * //
		 */
//		Scanner scan = new Scanner(System.in);
//		System.out.println("Enter the String ");
//		String name = scan.next(); //madam 
//		String result = "";
//		for (int i = name.length() - 1; i >= 0; i--) {
//			result = result + name.charAt(i);
//		}
//		if (name.equals(result)) {
//			System.out.println("String is Palindrome:");
//		} else {
//			System.out.println("String is not Palindrome");
//		}

		/*
		 * ount Vowels and Consonants: Write a function that counts the number of vowels
		 * and consonants in a given string. Example: "hello" -> Vowels: 2, Consonants:
		 * 3
		 */
//		int vowelCount = 0;
//		int consonantCount = 0;
//		String name = "mohan pilantre";
//		for (int i = 0; i < name.length(); i++) {
//			char ch = name.charAt(i);
//			if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u') {
//				vowelCount++;
//			}
//			if (ch != 'a' || ch != 'e' || ch != 'i' || ch != 'o' || ch != 'u') {
//				consonantCount++;
//			}
//		}
//		System.out.println("Vowel Count is : " + vowelCount);
//		System.out.println("Consonant count is : " + consonantCount);

		/*
		 * String to Uppercase: Write a function that converts all characters in a
		 * string to uppercase. Example: "hello" -> "HELLO"
		 */

//		String name = "Mohan";
//		System.out.println(name.toUpperCase());

		/*
		 * Find Substring: Write a function that checks if a given substring is present
		 * within a string. Example: "hello", "ell" -> true
		 */
		
//		String s1 = "AMIT";		
//		String s2 = "";	
//		System.out.println(s1.compareTo(s2));

//		String s1 = "I am a developer";		
//		String s2 = "";		
//		System.out.println(s1.compareTo(s2)); //count thr space 
	
	}
}
